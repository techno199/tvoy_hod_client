import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
    VkAuth: {
        background: '#fff',
        width: 200,
        minHeight: 133,
        borderRadius: 3
    },
});