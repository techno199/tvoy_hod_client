import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
    AddNewUserBg: {
        height: '100%',
        padding: '25px 40px',
        background: '#f6f6f6'
    },
});