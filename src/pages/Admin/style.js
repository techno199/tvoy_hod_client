import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
    authUser: {
        display: 'flex',
        minHeight: '100vh'
    },
    wrapRoute: {
        flexGrow: 1,
    }
}); 